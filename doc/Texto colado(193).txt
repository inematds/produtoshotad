What if you could start with a single photo of a real product and end up with an entire ad campaign, studio quality product images, multiple ad creatives, five short form video ads, all without binging a photographer or opening a video editor. And that waffle is finally practical because AI agents like cloud code can now orchestrate image generation, quality checks, voice overs, and video assembly using tools like a sitd and felai instead of making you jump between like 10 different apps.
So in this video, we are going to show you how to build that workflow from scratch, compare a fully automated pipeline against an AI assistant one, and explain why the hardest part is not generating beautiful ads. is actually keeping your product accurate from beginning to end, even if you have never built an AI automation before.
So, in order to build an AI powered product advertising pipeline, just follow these four steps. So, first, generate a premium hero image from a single real product photo while preserving the actual product details. And second, expand that into multiple product shots and advertising variations that all stay officially consistent.
And third, animate those images into short form video clips using like sitdowns and then generate a complete voice over automatically. And finally, let clude code assemble everything into finished advertisements or compare that against a workflow where a human adds the final polish.
So today we are building exactly that five AI generated product images and five vertical video advertisements from just one product photo. We are also comparing a fully automated workflow against an AI assisted workflow using the exact same generate assets. So you can see where automation is already excellent and where human judgment still adds value. Let's get into it.
So everything starts inside CL code and before we generate a single image, we need to give CL the workflow it will follow. So this project already includes a set of markdown files that define every single stage of the pipeline. And now let's take a quick look at how everything is organized. So here we are inside claude code and you can see we have a couple of markdown files prepared for this project.
And the first one is claude.mmd. You can think of this as the master playbook because it explains what we are building like a workflow that takes a single product photo and turns it into five product images and five video ads. So it will also tell cl which skills to load, how the pipeline is structured, the bruise should never break and how the automated and AI assisted workflows branch at the very end.
So in other words, this file gives cl the overall context before it writes a single line of code. Next, let's look at one of the custom skills we created for this project, the product consistency skill. So this is probably the most important skill in the entire workflow because it solves AI's biggest problem which is product drift and it tells Claude to always attach the original reference photos before every generation.
So we can keep prompts short and focused and only change one thing at a time. Fidate a single image before generating batches and most importantly run a quality check before anything moves to the next stage. And the whole purpose for this skill is making sure the product still looks like the real product from the beginning to end. So next we have the add pipeline skill.
And this one is responsible for the orchestration as it will tell CL the order everything happens in like from generating the hero image, expanding into product shots, creating the video scenes, generating the voice over and finally branching into either the fully automated hybrid frames workflow or the AI assisted editing workflow.
So it is essentially the road map for the entire automation. So those are the two most important files for this project. And we also have three more markdown files here. The 11lap setup and fail setup files are really just reusable build prompts. So instead of rewriting the same setup instruction every single time, we packaged them into markdown files so Claude can execute them whenever we need them.
And if you want to build this yourself, it also makes copying the workflow much easier. And all of these files will be available in our community. You can find a link in the description below as well. And finally, we have the hyperframes production skill. And if you watched our previous hyperframes video, this one will look familiar to you because we are simply reusing it here.
And it is a custom production skill built around the ripples of the hyperframes launch videos. Now that everything is set up, we can start building the entire workflow. And a nice thing is that we do not need to like manually explain anything to Claude. Everything it needs to know is already inside those markdown files.
And the first thing I'm going to do is switch the model over to set since this is a very straightforward implementation task. And I will also set the reasoning effort to medium. And now we can simply say something like, hey, can you please read the code MD file and implement this? And then you can tag the failed setup backdown file and hit enter and let CL do its thing.
So that one is finished. Now we do exactly the same thing for 11 laps and tag the 11lap setup markdown file. Send the prompt and wait for CL to complete the implementation. All right. So after about 2 minutes everything is finished and that is literally the build process. The only thing left is creating your EMV file and we have already included an example with the required environment variables.
So just copy those into your own file and then paste it in your val AI API key and your 11 labs API key. That's it. So everything starts with one product photo. And the goal is not just making it look better. It is keeping the product consistent across every single generated image. And that is where most AI workflows break down.
So let's see how Claude handles it. So you can see that we have already prepared two base prompt images here inside the products folder. The first one is a digital alarm clock from an Amazon listing and the second one is a meta VR headset. And now remember prompt engineering is already built into the product consistency skill.
So we do not need to spend time writing a massive highly curated prompt just to get good results. So instead we can use a simple initiate prompt like this. and tag the product photo we want Claude to work on and just hit enter. That's it. From here, we just need to wait for a bit. So, generating the images may take a little while because Claude is not only creating five product shots, it is also running the quality control checks defined in the workflow before moving on.
All right, so after a few minutes, everything is finished. And here are the five production shots we have. So, personally, I think these look pretty good, especially considering they only cost around like 2.5 cents per image to generate. And these are already usable for things like e-commerce product listings, landing pages, display ads, and later on.
And we're going to use this exact images to create our video advertisements. And now, for image ads specifically, you have two different approaches. The first is to automate the entire process like we did and CL generated the product shots then used the Adobe MCP tools together with HTML and CSS to automatically produce these finished ad creatives and the second option is to keep the product generation automated but do the final design yourself.
So this is a pretty good and valid workflow. So the AI already gives you a strong product image. The human just simply needs to add the final creative direction and branding. So now the images are ready. Let's actually turn them into video ads. And now if you are wondering where the script scenes empty file comes from. It's actually very easy to make.
You just simply need to open quot on the web or desktop app. Just paste in the prompt we have provided and ask it to generate a storyboard. From there you can just iterate on the scenes until you are happy with them. Then ask court to write a like voice over script that matches the final sequence. You can save that as your markdown file and you're ready to use it in the workflow.
And once everything's ready, we just need to give cord our prompt and tag both the product folder and the script sins.md file and then let it run. So this process is going to take a while. So just let it do its thing. And we'll come back once everything has finished generating. Great. After a while, everything's ready. Let's check out the first one.
So this one came up really well. So for a lot of e-commerce stores, this is already more than good enough to run on platforms like Meta Ads or Tik Tok ads. Now does that mean it's better than hire a professional editor? Not necessary for all cases. For example, like a skilled editor using Caput or Premier Pro will also certainly produce a more polished and more curated advertisement.
And that's why we actually think this workflow is strongest when paired with traditional editing like not replacing it entirely. So you can use hyperframes to automatically produce your simpler product ads and then send the more important or more complex campaigns to your editor. So you can get the speed and cost effectiveness of automation where it makes sense while still keeping a human touch where it matters the most.
And the real shift here is in how you spend your budget. So instead of paying an editor to create every single ad, you only outsource the videos that genuinely benefit from additional creative work and that means more of your budget can go towards actually like running ads instead of producing them. Now considering the AI generated version only cost a few val AI credits, a few 11 laps credits and a $20 cla pro subscription, it becomes a very compelling workflow for businesses that need to produce product ads at scale. So
with everything finished, let's wrap it up. So here is what this workflow actually delivers. Just one real product photo becomes five professional product shots and five complete short form video ads. And Claude Code orchestrates the entire pipeline using a set of reusable markdown workflows.
Faili and Sid dance generate officials. 11 Labs creates the voice over. And you can either let Hyperframes assemble the final ads automatically or just finish it yourself in Caput or Premier Pro. So the biggest shift here is not that completely AI replaces creative work is that AI now handles the repetitive 80% leaving you to decide where human creativity actually adds value.
So you can just automate your simpler ads, spend time polishing your biggest campaigns and put more of your budget into running ads instead of just producing them. So remember to try this yourself and you can find all the resources available in our community. You can find the links in the description below as well. And again, if there are other cloud code workflows you want us to cover, just let us know in the comment section below.
And if you want more in-depth AI video tutorials like this and to actually make more money with AI with a group of professional founders or builders, feel free to join our any no code community as well. And as always, if you found this video helpful, hit the like and subscribe button for more video like this in future. I'll see you in our next