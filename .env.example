# Worker 1 — ad-pipeline-agnes (custo US$ 0)
# Valor real em ~/projetos/openpcbot/.env — carregar em runtime, não copiar aqui.
AGNES_API_KEY=

# Worker 3 — ad-pipeline (Fal.ai / Seedance)
# Valor real em ~/projetos/wifi/.env — carregar em runtime, não copiar aqui.
FAL_KEY=

# Worker 3 — ad-pipeline (voiceover)
# Valor real em ~/projetos/openpcbot/.env — carregar em runtime, não copiar aqui.
ELEVENLABS_API_KEY=
